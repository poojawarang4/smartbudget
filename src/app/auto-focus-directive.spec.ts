import { AutoFocusDirective } from './auto-focus-directive';

describe('AutoFocusDirective', () => {
  it('should create an instance', () => {
    const directive = it('should create', () => {
  expect(true).toBeTrue();
});

    expect(directive).toBeTruthy();
  });
});
